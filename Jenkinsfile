pipeline {
    agent any {
        
        stages {
          stage('checkout'){
              steps{
                  checkout scm
              }
          }
          
          stage('build image and push container')
        }
    }
    
    
}
